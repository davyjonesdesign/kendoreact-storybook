import { create } from 'storybook/theming';

export default create({
  base: 'light',
  brandTitle: 'w-kendo theme',
  brandUrl: 'https://ui.wheels.com',
  brandImage: 'wheels.svg', // Use absolute path from public
  brandTarget: '_self',
});